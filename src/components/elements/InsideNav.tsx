import * as React from "react";
import { useState } from "react";
import Select from "react-select";
const InsideNav: React.FunctionComponent = (props) => {
  return <div className="h-24 w-full bg-red-500"></div>;
};

export default InsideNav;
